/* ============================================================================
   IRON INVEST — фактические статусы документов по объектам.

   Заполняется только по тому, что физически есть в архиве. Пустой объект
   означает «по объекту ничего не собрано» — это тоже факт, и он полезнее
   отсутствия записи.

   Статусы: none | requested | received | verified | expiring | expired | na
   «received» — файл в архиве. «verified» ставит только юрист или инженер,
   поэтому сам по себе появиться не может.

   Даты — по дате файла в архиве.
   ============================================================================ */
(function () {
  const D = (s, at, note) => ({ s: s, at: at || null, note: note || null });

  window.IronDocStatus = {
    'plaka-1': {
      '0.4':  D('received', '2026-08-04', 'Топоплан с координатами ЕГСА87 и вертикальными долями Ε3, Ε41, Ε42'),
      '0.5':  D('received', '2026-08-04', 'Видеофиксация объекта, шесть роликов'),
      '0.7':  D('received', '2026-08-04', 'Инвест-разбор KH371: цена, площади, зона, ограничения'),
      '0.8':  D('received', '2026-08-04', 'Предварительный расчёт вошёл в тот же разбор'),
      '1.9':  D('received', '2026-08-04', 'Тот же топоплан: ЕГСА87, границы, ΤΜΗΜΑ ΑΑ 49,30 м²'),
      '1.18': D('requested', '2026-08-04', 'Есть поэтажный план цоколя; самого разрешения на строительство нет')
    },
    'plaka-2': {
      '0.4':  D('received', '2026-08-04', 'Топоплан с границами участка и контуром дома'),
      '0.5':  D('received', '2026-08-04', 'Видеофиксация объекта, два ролика'),
      '1.9':  D('received', '2026-08-04', 'Тот же топоплан'),
      '1.18': D('requested', '2026-08-04', 'Есть планы цоколя и первого этажа; разрешения на строительство нет')
    },
    'plaka-3-taverna': {},
    'plaka-4': {}
  };
})();
